services:
  postgres:
    image: 'postgres:17.7'
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: '${POSTGRES_PASSWORD}'
      POSTGRES_DB: postgres
    volumes:
      - 'postgres_data:/var/lib/postgresql/data'
      - './reporting-bootstrap.sql:/docker-entrypoint-initdb.d/10-reporting-bootstrap.sql:ro'
    networks:
      coolify-shared:
        aliases:
          - postgres
    healthcheck:
      test:
        - CMD-SHELL
        - 'pg_isready -U n8n -d n8n'
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped
volumes:
  postgres_data: null
networks:
  coolify-shared:
    external: true
