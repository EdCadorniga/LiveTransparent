# Environment and secrets
.env
**/.env
config.toml
config.toml.bak
*.env.local
*.env.*.local

# Node / JS
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Runtime and cache
.npm-cache/
*.log
*.tmp
*.cache
**/__pycache__/
.tmp/
.tmp_*
tmp_*
_exec_*.json
_wf_*.json
temp-*.json
temp.json
response.json
workflow_temp.json
workflow_updated.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Untracked n8n workflow backups
Backup of n8n workflows UNTRACKED/

# Browser MCP debug artifacts
.playwright-mcp/
screenshot_stale_executions.jpg

config.toml.bk

# n8n workflow source files (may contain webhook URLs / secrets)
n8n/workflows/

# LiveTransparent monitor runtime state (dedup + logs; no secrets but ephemeral)
.monitor/

# Review-only landing page prototype (not part of the main repository)
/Sample Landing Page/

# Local operator helpers and reusable probes (may contain machine-specific paths)
/local-scripts/

# Historical n8n exports and local workflow archives
/local-archive/
/n8n/backups/
/n8n/workflows/*.ts
/n8n/workflows/*.json
/n8n/voice-agent/**/*.json
/n8n/voice-agent/*.mjs
