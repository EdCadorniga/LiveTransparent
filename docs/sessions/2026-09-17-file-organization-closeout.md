# File Organization and Cleanup Closeout

Date: 2026-09-17

## Objective

Organize the LiveTransparent repository, remove disposable local artifacts, preserve important operational files, and document the resulting file boundaries.

## Completed

- Removed approximately 140 MB of disposable local artifacts:
  - `.tmp/`
  - `.npm-cache/`
  - `.playwright-cli/`
  - `.playwright-mcp/`
- Added `.playwright-cli/` to `.gitignore`; existing `.monitor/` state was preserved.
- Moved all 117 versioned root scripts into categorized `scripts/<domain>/` directories.
- Added `scripts/README.md` and the complete file-level map at `docs/repository-file-map.md`.
- Apollo September 2026 preparation/import outputs and reconciliation logs are grouped under `data/runs/2026-09-16-apollo-vp-ghl/`.
- Updated documentation and script references to the new paths.
- Removed generated `__pycache__/` directories from versioned and local helper areas.
- Preserved the identifying `repeated linkedin messages.png` screenshot as intentionally untracked and did not relocate it.

## Verification

- `git diff --check` passed; only normal Windows LF/CRLF warnings were reported.
- JavaScript syntax checks passed for the moved JavaScript scripts.
- 117 script files are present under categorized directories; no root-level scripts remain.
- The Apollo bundle contains 16 files and the required notes/source snapshots are present.
- No stale root-script references were found in the scanned documentation/source files.
- No cache or generated bytecode directories remain.

## Known pre-existing defects

Python compilation still reports syntax errors in these retained one-off scripts:

- `scripts/instagram/batch_instagram_validate_and_send.py`
- `scripts/social-reporting/fix_brands_code.py`
- `scripts/utilities/x.py`

They were not behaviorally changed during this organization pass and require a separate debugging task before execution.

## Worktree and safety state

- Branch: `codex/social-outreach-sync`, tracking `origin/codex/social-outreach-sync`.
- The worktree contains pre-existing dirty changes in addition to this organization pass; do not stage everything blindly.
- No commit or push was performed.
- No production workflow, CRM record, campaign, sender, deployment, service, or external system was changed.
- `.env` contents and credentials were not read or copied.

## Next session

1. Review the organization diff and stage only the intended cleanup/documentation changes.
2. Decide whether the three syntax-invalid one-off scripts should be repaired, archived, or retired; do not delete them solely based on their names.
3. Run a dedicated secret scan before any broad staging operation.
4. Commit only after the selected diff passes review; do not push or deploy without separate approval.
